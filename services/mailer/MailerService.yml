#  \
#  \\,
#   \\\,^,.,,.                     Zero to Hero
#   ,;7~((\))`;;,,               <zerotohero.dev>
#   ,(@') ;)`))\;;',    stay up to date, be curious: learn
#    )  . ),((  ))\;,
#   /;`,,/7),)) )) )\,,
#  (& )`   (,((,((;( ))\,

apiVersion: v1
kind: Service
metadata:
  namespace: fizz
  name: store
spec:
  selector:
    app: store
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9004
  type: LoadBalancer
